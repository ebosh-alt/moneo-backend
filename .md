[P1] Spoofable forwarded headers bypass HTTPS/rate limits" body="Middleware uses Gin ClientIP and trusts raw X-Forwarded-Proto/X-Forwarded-Ssl without trusted proxy configuration. A direct client can rotate X-Forwarded-For to evade auth rate limits and send X-Forwarded-Proto: https to bypass HTTPS enforcement." file="/Users/eboshit/projects/moneo/backend/internal/transport/http/auth_security.go" start=157 priority=1 confidence=0.88

[P1] API binary does not start the new API" body="cmd/api only calls bootstrap.NewAPI, and NewAPI only returns a struct. No router, DB pool, auth services, middleware, or HTTP listener are wired, so deploying this branch starts no server and exposes none of the auth endpoints." file="/Users/eboshit/projects/moneo/backend/cmd/api/main.go" start=10 priority=1 confidence=0.95

[P1] Browser logout cannot revoke refresh session" body="The refresh cookie is scoped to /auth/refresh, but logout reads the refresh token from /auth/logout. Browsers will not send that cookie to /auth/logout, and because it is HttpOnly browser clients cannot copy it into the JSON body; logout can return OK while the server session remains active." file="/Users/eboshit/projects/moneo/backend/internal/transport/http/auth_handler.go" start=22 priority=1 confidence=0.9

[P1] Password reset and email verification are not atomic" body="ResetPassword consumes the token, then updates the password, then revokes sessions as separate calls. VerifyEmail also consumes the token before updating the user. Any later failure can leave a consumed token without the intended account change, or changed password with old sessions still alive." file="/Users/eboshit/projects/moneo/backend/internal/app/identity/post_mvp.go" start=200 priority=1 confidence=0.9

[P2] Refresh can race with session revocation" body="Refresh loads an active session, then TouchLastUsedAt updates by id only. If logout/reset revokes the session between those steps, refresh can still touch the revoked row and issue an access token. Make the active-session lookup/touch atomic or re-check revoked_at/expires_at in the update." file="/Users/eboshit/projects/moneo/backend/internal/infra/postgres/auth_repositories.go" start=453 priority=2 confidence=0.82

[P2] Expired sessions still authenticate access tokens" body="AccessAuthService checks user match and RevokedAt, but not ExpiresAt. A JWT issued shortly before refresh-session expiry can continue authorizing protected routes after the session lifetime has elapsed." file="/Users/eboshit/projects/moneo/backend/internal/app/identity/access_auth.go" start=67 priority=2 confidence=0.9

[P2] Malformed session id can become 500" body="DELETE /auth/sessions/:sessionId casts arbitrary route text to SessionID and passes it to Postgres UUID comparison. Invalid UUID syntax is likely returned as an internal error instead of a 400/404." file="/Users/eboshit/projects/moneo/backend/internal/transport/http/auth_handler.go" start=390 priority=2 confidence=0.78

[P2] APP_ENV default disables intended HTTPS enforcement" body="docker-compose defaults APP_ENV to prod, while DefaultAuthSecurityConfig only treats production as production. A compose-based deployment can miss the HTTPS-required guard." file="/Users/eboshit/projects/moneo/backend/docker-compose.yml" start=25 priority=2 confidence=0.9

[P2] Architecture import guard is stale" body="go.mod now uses module moneo, but the architecture test still scans for moneo/backend/internal/domain/. The test passes while no longer enforcing current domain import boundaries." file="/Users/eboshit/projects/moneo/backend/internal/architecture/imports_test.go" start=21 priority=2 confidence=0.92

**По направлениям**
1. `Уязвимости безопасности`: главный blocker — spoofable forwarded headers. Дополнительно: compose публикует Postgres с дефолтными `video:video`, `APP_ENV=prod` отключает HTTPS guard, expired sessions remain accepted.
2. `Качество кода`: нет app-level transaction boundary для multi-step auth workflows; transport читает env напрямую; `NewOneTimeToken` принимает неизвестные purpose; stale architecture test.
3. `Баги`: API binary exits without serving; browser logout не ревокает session; malformed session id может давать 500; compose `app` не соберётся без Dockerfile/config.
4. `Race conditions`: reset/verify token flows не atomic; refresh-vs-revoke race; production interleavings не покрыты Postgres integration tests.
5. `Нестабильные тесты`: endpoint fixture зависит от реального `APP_ENV`; при `APP_ENV=production` plain `httptest.NewRequest` получает `https_required` вместо ожидаемых 200/201.
6. `Поддерживаемость`: auth route policy продублирована между router/rate-limit/HTTPS/logging maps; `auth_handler.go` уже слишком широкий; nil notifiers silently become no-op; bootstrap не отражает production wiring.

**Проверки**
`go test ./...` прошёл. Один агент подтвердил `go run ./cmd/api` завершается сразу с `0`, что подтверждает проблему runtime wiring.